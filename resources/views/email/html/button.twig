<table class="action" align="{{ align ?? 'center' }}" width="100%" cellpadding="0" cellspacing="0" role="presentation">
    <tr>
        <td align="{{ align ?? 'center' }}">
            <table width="67%" border="0" cellpadding="0" cellspacing="0" role="presentation">
                <tr>
                    <td>
                        <a href="{{ action }}" class="button button-{{ style ?? 'primary' }}" target="_blank"
                            rel="noopener">
                            <span class="button-text">{{ text }}</span>
                            <span class="button-text">
                                →
                            </span>
                        </a>
                    </td>
                </tr>
            </table>
        </td>
    </tr>
</table>
